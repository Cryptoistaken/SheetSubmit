org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
android.nonTransitiveRClass=true
# GitHub Packages credentials for org.telegram:login-sdk (Telegram Login SDK)
# Set via ~/.gradle/gradle.properties or env: GITHUB_USERNAME/GITHUB_TOKEN (or GITHUB_ACTOR)
# Do not hardcode secrets here — resolved in settings.gradle via findProperty/env fallback
# gpr.user=
# gpr.key=
